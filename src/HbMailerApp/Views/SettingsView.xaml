<UserControl x:Class="HbMailerApp.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HbMailerApp.Views"
             
             mc:Ignorable="d" 
             
             d:DesignHeight="300"
             d:DesignWidth="300">
  <StackPanel>
    <TextBox Margin="5" Text="{Binding Path=ApiKey, UpdateSourceTrigger=PropertyChanged}"></TextBox>
    <TextBox Margin="5" Text="{Binding Path=Hostname, UpdateSourceTrigger=PropertyChanged}"></TextBox>
    <TextBox Margin="5" Text="{Binding Path=Database, UpdateSourceTrigger=PropertyChanged}"></TextBox>
    <Button Width="100" Margin="5" HorizontalAlignment="Right" Command="{Binding Path=ValidateCommand}">Validate</Button>
  </StackPanel>
</UserControl>
