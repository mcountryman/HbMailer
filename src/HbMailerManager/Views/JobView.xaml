<UserControl x:Class="HbMailer.Views.JobView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HbMailer.Views"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             
             mc:Ignorable="d" 
             
             d:DesignHeight="450"
             d:DesignWidth="400">
  <StackPanel>
    <TextBox Margin="5" Controls:TextBoxHelper.Watermark="Name" Text="{Binding AppContext.Job.Name}"></TextBox>
    <StackPanel FlowDirection="RightToLeft" Orientation="Horizontal">
      <Button Margin="5" Width="100" Command="{Binding CancelCommand}">Cancel</Button>
      <Button Margin="5" Width="100" Command="{Binding DeleteCommand}">Delete</Button>
      <Button Margin="5" Width="100" Command="{Binding SaveCommand}">Save</Button>
    </StackPanel>
  </StackPanel>
</UserControl>
